//! Metodos de string

// 'f','e','r'
//  0   1   2

//? Propiedades
/*  Devuelve la longitud de un string
    length */
    
//? Metodos
/* Devuelve el caracteres del index indicado
string.charAt(i) */

// toUpperCase()

//toLowerCase();

//subString(posicion inicial, posicion final)

//slice(posicion inicial, posicion final)
//Acepta numeros negativos (lee del final al inicio)


//indexOf(valor a buscar, ?posicion inicial)

//replace(valor a buscar, valor de cambio) <- primer valor

//replaceAll(valor a buscar, valor de cambio)

//trim() remueve espacios en blanco al inicio y final
